# Base configuration
X_BUILD_BASE_CFG="mips"

KERNCONF=RSPRO_STANDALONE

# The configuration load/save script needs to know the
# configuration partition name and location.
BIN_CFG_PARTITION="/dev/redboot/cfg"
BIN_CFG_SIZE="65536"

UBNT_ARCH="RSPRO"
UBNT_VERSION="RSPRO.ar7100pro.V1"

# Building the firmware image
X_BUILD_BUILD_IMG_DEFAULTS="fetchpkgs fullroot makepkgs addpkgs fullfsimage ubnt"

