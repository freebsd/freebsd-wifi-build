# Base configuration
X_BUILD_BASE_CFG="mips64"

KERNCONF=MALTA64

X_ROOTFS_DEV="/dev/ada0"

X_FULL_FSSIZE="4294967296"

# Building the firmware image
X_BUILD_BUILD_IMG_DEFAULTS="fullroot fullfsimage"
